'use strict';

angular.module('hotelApp')
    .factory('School', function ($resource, DateUtils) {
        return $resource('api/schools/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
